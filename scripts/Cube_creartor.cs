using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Cube_creartor : MonoBehaviour
{
    public int size = 1;
    // Start is called before the first frame update
    void Start()
    {
        line(); 
    }

    // Update is called once per frame
    void Update()
    {
        line();
    }
    void line()
    {
      
           // for (int i = 0; i < size; i++)
            //{
               // GameObject amigo = GameObject meh = GameObject.CreatePrimitive(PrimitiveType.Sphere);
                //meh.transform.position = new Vector3(i, 0, 0);
               /// amigo.AddComponent<RigidBody>();
               // amigo.AddForce(transform.forward * 8);

           // }
        
        
    }
}
